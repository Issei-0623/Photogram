<div class="feed-page" data-controller="accordion-menu">
  <%= render "feed_header" %>

  <%= render "feed_posts",
        timeline: params[:type] == "timeline",
        posts: @posts,
        popular_posts: @popular_posts,
        recent_posts: @recent_posts %>
</div>
